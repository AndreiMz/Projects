<div class='row justify-content-center'>
	<div class='row'>
		<% @all_favs.each do |(id,favs)| %>
		<div class='card'>
			<div class='card-body'>
				<div class='card-text'>
					<%= User.find(id).username %>
					<br>
					<%= link_to "Favorites of this user",u_fav_path(id) %>
				</div>
			</div>
		</div>
		<% end %>
	</div>
</div>